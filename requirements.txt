# Core ML
torch>=2.0
lightgbm>=4.0
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
shap>=0.44

# Experiment tracking & monitoring
mlflow>=2.9
evidently>=0.4

# AWS / deployment
boto3>=1.34
sagemaker>=2.200

# Visualization (notebooks)
matplotlib>=3.7
seaborn>=0.13

# Dev / test
pytest>=8.0

# NOTE: apache-airflow is intentionally NOT pinned here. It has heavy,
# version-pinned transitive deps and is best installed in its own
# environment via the official constraints file:
#   pip install "apache-airflow==2.9.3" \
#     --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.9.3/constraints-3.11.txt"
